import { createSlice } from "@reduxjs/toolkit";

const DiariesReducer = createSlice({
  name: "diariesReducer",
  initialState: {},
  reducers: {},
  extraReducers: {},
});

export default DiariesReducer;

// export const {} = DiariesReducer.actions;

// export const selectLoginData = (state: any) => ({});
